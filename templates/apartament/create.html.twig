{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create Apartament</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Create</button>
    {{ form_end(form) }}

{% endblock %}